#include <bits/stdc++.h>
#define endl "\n"

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;
    cin >> n;
    if (n)
    {
        cout << "Leading the Way to the Future" << endl;
    }
    else
    {
        cout << "YONSEI" << endl;
    }
    return 0;
}